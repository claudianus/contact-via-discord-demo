<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
  <title>Contact</title>
</head>
<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-8">
        @if(flashMessage('success'))
          <div class="alert alert-primary" role="alert">
            {{ flashMessage('success') }}
          </div>
        @endif
        <h1>Contact</h1>
        <form action="{{route('contact.send')}}" method="post">
          {{ csrfField() }}
          <div class="row mb-1">
            <div class="col pr-1">
              <input type="text" name="name" class="form-control" placeholder="Put your name here" maxlength="50" required>
              @if(hasErrorFor('name'))
                <small class="form-text text-muted text-danger">{{getErrorFor('name')}}</small>
              @endif
            </div>
            <div class="col pl-0">
              <input type="email" name="email" class="form-control" placeholder="Put your email here" maxlength="50">
              @if(hasErrorFor('email'))
                <small class="form-text text-muted text-danger">{{getErrorFor('email')}}</small>
              @endif
            </div>
          </div>
          <textarea name="content" cols="30" rows="10" class="form-control" placeholder="Put your content here" maxlength="1800" required></textarea>
          @if(hasErrorFor('content'))
            <small class="form-text text-muted text-danger">{{getErrorFor('content')}}</small>
          @endif
          <button type="submit" class="btn btn-primary mt-1">Submit</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
